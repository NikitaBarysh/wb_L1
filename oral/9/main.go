package main

/*
	var m map[int]int -объявление переменной m, она будет nil
	var m make(map[int]int) - объявление переменной не nil
	n := map[int]int{} - объявление и инициализация
	c := make(map[int]int) -  объявление и инициализация пустой мапы


	var s []int - объявление переменной равной nil
	k := []int{0} - инициализация переменой, и присваивание значения
	d := make([]int, 0) - инициализация нулевой длины
	d := make([]int, 2, 4) - инициализация с длиной 2 и емкостью 4
*/
